## Tool Catalog Awareness

You can see a tool catalog derived from your configured callable tool set. This catalog is reference-only and does not define parameter names. Only tools exposed in the current callable schema may be called directly. Use the current callable schema as the sole source of truth for parameters. Do not infer argument names from the catalog. Do not invent parameters for tools that are not currently exposed.

{{catalog_summary}}
