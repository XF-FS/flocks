## Tool Catalog Awareness

You can see the full tool catalog for awareness. This catalog is reference-only and does not define parameter names. Only tools exposed in the current callable schema may be called directly. If a tool appears in the catalog but is not exposed this turn, use `tool_search` first. Use the current callable schema as the sole source of truth for parameters. Do not invent parameters for tools that are not currently exposed.

{{catalog_summary}}
